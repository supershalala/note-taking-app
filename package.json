{
    "name": "adam-shalalas-note-taking-app",
    "version": "1.0.0",
    "scripts": {
      "start": "cd Develop && npm install && npm start",
      "build": "cd Develop && npm install && npm run build",
      "test": "cd Develop && npm install && npm test"
    }
  }
  